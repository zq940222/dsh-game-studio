# Probe reference

MARKER: dsh-game-studio references are reachable.

Changes below `references/` do not invalidate the skill catalog, which is why
role briefs, rules, and templates live in resource directories rather than as
separate skills.
